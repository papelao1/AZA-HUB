rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All collections: any authenticated user (including anonymous) can read/write.
    // Access control is enforced by the application's custom auth layer.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
